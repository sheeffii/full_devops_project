name: Infrastructure CI/CD

on:
  push:
    branches: [ main ]
    paths: [ 'infrastructure/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'infrastructure/**' ]
  workflow_dispatch:
    inputs:
      action:
        description: 'Action'
        required: true
        default: 'deploy'
        type: choice
        options:
          - deploy
          - destroy

jobs:
  quick-check:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: Validate
        working-directory: ./infrastructure/dev
        run: terraform init -backend=false && terraform validate

  build-ami:
    if: github.event_name != 'pull_request' && (github.event.inputs.action != 'destroy' || github.event.inputs.action == null)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
      - uses: actions/cache@v4
        with:
          path: ~/.cache/packer
          key: ${{ runner.os }}-packer-${{ hashFiles('infrastructure/packer/**/*.pkr.hcl') }}
      - run: make packer-build
        working-directory: ./infrastructure

  do-deploy:
    if: github.event_name != 'pull_request' && (github.event.inputs.action != 'destroy' || github.event.inputs.action == null)
    needs: build-ami
    runs-on: ubuntu-latest
    environment: dev  # ← enables approval gates
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
      - uses: hashicorp/setup-terraform@v3
      - uses: actions/cache@v4
        with:
          path: ~/.terraform.d/plugin-cache
          key: ${{ runner.os }}-tf-${{ hashFiles('infrastructure/**/*.tf') }}
      - run: make deploy-testing
        working-directory: ./infrastructure
      - name: Show Outputs
        working-directory: ./infrastructure/dev
        run: terraform output

  destroy-infra:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
      - uses: hashicorp/setup-terraform@v3
      - run: make destroy AUTO=yes
        working-directory: ./infrastructure